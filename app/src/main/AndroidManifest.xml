<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.mukundmurarka.attendance">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Student_Ragistration" />
        <activity android:name=".Main3Activity" />
        <activity android:name=".Start_page" />
        <activity android:name=".Teacheractivity" />
        <activity android:name=".AddStudent" />
        <activity android:name=".StudentList" />
        <activity android:name=".TEACHERLIST" />
        <activity android:name=".Viewattendance" />
        <activity android:name=".AddAttendance" />
        <activity android:name=".TotalAttendance"></activity>
    </application>

</manifest>